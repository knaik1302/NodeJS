(function(exports, require, module, __filename, __dirname){
    const fs= require('fs');
    const name = "kartik";
    console.log(name);
}); // bcz of this module wrapper all the content inside are private for outside

// console.log(__dirname);
// console.log(__filename);